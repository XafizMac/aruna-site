import axios from "axios";

export default async function checkAdminStatus(email: string | undefined) {
    try {
        // const response = await axios.post("/api/check-admin", { email })
    }
    catch (e) {

    }
}