"use client"

import { Button } from "@geist-ui/core";
import { FC } from "react";

const Admin: FC = () => {
    return(
        <Button placeholder={undefined} onPointerEnterCapture={undefined} onPointerLeaveCapture={undefined}>Admin page</Button>
    )
}

export default Admin;